import { Loading } from '../common/spinner';

export const Recipe = () => {
    return (
        <section id="recipes-page">
            <h1>All Recipes</h1>

            <Loading />
        </section>
    );
};